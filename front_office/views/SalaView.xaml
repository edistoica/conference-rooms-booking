<UserControl x:Class="Proiect_BD.Front_Office.Views.SalaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Proiect_BD.Front_Office.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid x:Name="salaGrid" ItemsSource="{Binding List}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="salaGrid_SelectionChanged">
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="Id" Binding="{Binding Sala_Id}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Floor" Binding="{Binding Floor}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Occupied" Binding="{Binding Occupied}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
